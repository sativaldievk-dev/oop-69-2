# Команды для регистрации ваших данных

git config --global user.name "sativaldievk-dev"

git config --global user.name "sativaldievk@gmail.com"

git init   (инициализация гита)
git remote add origin link_to_repository (добавление репозитория)
git status      (статус гита)
git add .   добавление файлов в буффер обмена
git commot -m "main.ру" сохранение всего буфера
git push origin main
