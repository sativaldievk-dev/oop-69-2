# Практический тест №1

Архитектура: handlers / keyboards / states / database.

Запуск:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 main.py
```

Токен вставить в `config.py`.
Команды: `/start`, `/menu`, `/add_product`, `/drinks`.
